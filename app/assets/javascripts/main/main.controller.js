(function () {
    "use strict";
    angular.module('charlestonBucketList')

    .controller('MainController', function (MainService) { //scope is going out, so best not to use//

        var mainCtrl = this;
    })
})();
