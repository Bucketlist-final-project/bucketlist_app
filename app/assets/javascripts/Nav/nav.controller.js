(function () {
    "use strict";
    angular.module('charlestonBucketList')

    .controller('NavController', function (NavService) { //scope is going out, so best not to use//

        var navCtrl = this;
    })
})();
